import React from "react";
// import Landing from "./Landing";



const Landing = ({robots, getBots, deleteHandler}) =>{    
    return(
        <div>
            {/* <Landing bots ={robots} getBots ={getBots} deleteHandler = {deleteHandler}/> */}
        </div>
    );
}

export default Landing;